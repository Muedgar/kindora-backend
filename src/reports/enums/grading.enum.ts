/**
 * @deprecated Use EGradingType from grading-type.enum.ts (Phase 1).
 * Kept for backward compatibility only; do not use in new code.
 */
export enum EGradingTypeLegacy {
  CATEGORICAL = 'CATEGORICAL',
  DESCRIPTIVE = 'DESCRIPTIVE',
}
